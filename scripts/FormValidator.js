class FormValidator {
  constructor(formValid, object) {
    this._validationConfig = object;
    this._form = formValid;
  }

  enableValidation() {
    //публичный метод который включает валидацию
  }
}
